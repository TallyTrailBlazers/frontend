'use strict';

describe('myApp.trailpage module', function() {

  beforeEach(module('myApp.trailpage'));

  describe('trailpage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var trailPageCtrl = $controller('TrailPageCtrl');
      expect(trailPageCtrl).toBeDefined();
    }));

  });
});
